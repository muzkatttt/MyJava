spring:
  application:
    name: my-shop-book
    issue:
      max-allowed-books: 3
      list:
        "item1": "1"
        "item2": "2"
  datasource:
    url: jdbc:h2:mem:test
    username: kk
    password:
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
server:
  error:
    include-message: ALWAYS